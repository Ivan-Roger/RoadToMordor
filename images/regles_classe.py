# coding=utf-8

import pygame

class Regles:
	def __init__(self,screen):
		self.screen = screen
		self.font = pygame.font.Font("alagard.ttf",20)

	def draw(self):
		texts = list()
		for line in 
		texts.append(self.font.render(line))
		self.screen.blit()

	def reset(self):
